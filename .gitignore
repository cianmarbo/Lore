.vscode
lore
lore.db
lore.db-shm
lore.db-wal
frontend/dist
frontend/node_modules
models/
plugin/

# Plugin config carries an LLM API key — keep local copy out of git.
# Use plugin/.mcp.json.example as the template.
plugin/.mcp.json
